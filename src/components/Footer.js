import React from "react";

const Footer = () => (
  <footer className="py-8 mt-12">
    <div className="section-container text-center text-sm text-gray-500">
      © {new Date().getFullYear()} Ramya Adepu — Built with React & Tailwind
    </div>
  </footer>
);

export default Footer;
